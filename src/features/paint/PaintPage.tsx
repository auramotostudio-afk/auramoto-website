import { Container } from '@/components/ui/Container';import { Button } from '@/components/ui/Button';import { ImagePanel } from '@/components/ui/ImagePanel';import { images } from '@/lib/site';
export function PaintPage(){return <section className="py-14 md:py-24"><Container><div className="grid gap-10 lg:grid-cols-[1fr_.95fr] lg:items-center"><div><p className="gold-label">Paint Correction</p><h1 className="display title-lg mt-8">From dull surfaces to controlled reflections.</h1><p className="bodycopy mt-7">Each panel is inspected, refined and finished with measured passes. The goal is not aggressive cutting — it is controlled clarity.</p><div className="mt-9"><Button href="/pricing">View Pricing</Button></div></div><ImagePanel src={images.correction} label="Paint Clarity" className="h-[44vh] min-h-[320px] md:h-[64vh]"/></div></Container></section>}
